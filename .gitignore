# Go build outputs
/bin/
/dist/

# Embedded SPA is generated by `make build-ui` (pnpm build). The
# directory is kept in git via .gitkeep so `go build` works; the
# generated bundle itself is not committed.
/internal/server/dist/*
!/internal/server/dist/.gitkeep
